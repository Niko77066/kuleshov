# styles/_disabled/ — 暂禁用的风格包

此目录下的包**未完成、暂不 offer 给生产**。`_` 前缀 = 非在用风格包，produce 开拍提问不列入可选清单。

| 包 | 禁用原因 | 复活条件 |
|---|---|---|
| `meme-ledger` | 候选包，尚未考片、未完成（2026-07-24 用户拍板先禁） | 完善后经 `/video-score` + 版权清单双重验收 → 移回 `styles/meme-ledger/` 并加回 produce 可用包清单 |

复活即：`mv styles/_disabled/<pack> styles/<pack>`，并在 `.claude/skills/produce/SKILL.md` §1.3 可用包清单加回。
