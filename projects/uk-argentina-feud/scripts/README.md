# ⚠ 死脚本 — 非管线、非能力包的一部分

本目录下的 `*.py` 是英阿片制作期的**一次性临时脚本**，**硬编码指向一个已删除的 worktree**
（`/Users/admin/kuleshov/.claude/worktrees/estee-lauder-night-video-7162e0/...`），**跑不起来、也不该跑**。

- 它们不是生产管线的一部分——管线在 `.claude/skills/produce/SKILL.md` + `kuleshov-ir` + `tools/`。
- 保留仅为历史留痕；消费本能力包时**忽略整个 `scripts/` 目录**。
- 需要复现该片的某步：走 `/produce` SOP 重跑，别用这些脚本。
