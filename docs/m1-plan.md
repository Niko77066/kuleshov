# M1 执行计划（2026-07-16 拍板版）

> 依据：2026-07-15 评审反馈 + 2026-07-16 作者裁决（[拍板记录](review-2026-07-15-response.md#️-拍板记录2026-07-16作者裁决覆盖下文中的对应建议)）。
> 总原则：**效果第一位，排期可让步**——首版当天出，质量不过评分线宁可返工延发，不硬发。选题、创意、信息密度是拿流量的生命线。

## 两个生产目标（7/17 下班前搞定）

| 方向 | 定义 | 风格包 | 发布 |
|---|---|---|---|
| A · 知识科普 3 分钟 | 对标万物电台《佛得角》形态（横屏 16:9、3–4 分钟、MG+实拍混排） | `pixel-chronicle`（首考） | 小红书 + 抖音，双平台发布包 |
| B · 荆华密算社媒视频 | 全球 AI 隐私/泄露热点新闻 → 专业解读 + 安全提醒 → 六场景产品价值（三段框架，48h 时效） | 现有包选用（daily-brief 优先，品牌色覆写），**不为首片新建包** | 企业号，过 compliance 判定框 |

## 7/17（五）当日 todo

**已提前完成（7/16 晚拍板立项）**
- [x] A 线选题拍板：**《英国和阿根廷：什么仇什么怨》**（时事驱动，用户改选，五候选让位，记 ledger）→ `projects/uk-argentina-feud/` 已立项（brief + film.json；research 调查中：世界杯对阵时效锚 + 史实核查）
- [x] B 线热点拍板：**B1+B2 并置**（OpenAI 7800 万对话库 × 拟人化办法 7/15 施行）→ `projects/openai-78m-logs/` 已立项（brief + research 8 条带出处 + film.json）

**上午 —— 双线开拍**
- [ ] A 线 research 定稿（时效锚必须核实：对阵场次与开球时间决定发布窗）
- [ ] 两线 script 过用户 ⏸（B 线同时过 compliance 判定框）→ TTS + timeline 定稿（音频先行）
- [ ] B 线品牌素材确认：荆华密算品牌色/logo 需业务侧提供，缺省用 daily-brief 默认色出 v1

**下午 —— 生产**
- [ ] 两线 storyboard（八项自查）+ anchors（N 选 1）⏸ → motion / compose 交错流水
- [ ] A 线实拍需求镜头兜底决策：检索 API 未到 → Seedance 纪录风空镜过缝合层 或 人工投喂素材（选哪条记 `ledger.decisions`）
- [ ] 关键镜头（hook/hero）并行 2 候选，选优（拍板保留项）

**傍晚 —— 出厂**
- [ ] review：L0 仪器证据 + 用户看片 ⏸ → `/video-score` 登记 **9 维分**（D8 创意 / D9 网感 首用）
- [ ] 双平台发布包（`/rednote-mentor` 场景 A Step 5）：3:4 封面 / 标题（含搜索词）/ 标签 / 描述
- [ ] 用户拍板后发布；两片若有一片质量不过线 → 效果第一，返工排 7/18 补发

**当日纪律**：工程项全部让路（IR API 不动工，film.json 照 M0 手编 + 手动自查）；一天双片是 M0 实测产能约 5 倍，若撞墙优先保 A 线完整出厂，B 线允许停在"成片待发布"。

## 待接入 API（用户提供，契约到手即接）

| API | 状态 | 说明 |
|---|---|---|
| 检索/实拍素材 API | ✅ **已闭环（2026-07-18，英阿片实测）** | 三层方案替代单一 API：Pexels/Pixabay（纯空镜）+ archive.org/Wikimedia 公有领域（叙事/证据位）+ **APIhub** `youtube_search→video_download`（时事新闻位）。知识包 `produce/references/footage-sourcing.md`（含 provenance/license 登记工序、空镜池五铁律）；凭据均在 `.env` |
| 服务器渲染 API | ✅ **通路验证完成（2026-07-20）**，切默认待下一部片复验 | A/B/C 三阶段全过（真片 65.7s 服务端 129s 渲完）；版本按请求装（0.7.3/0.7.57/0.7.60 实测均可）。真片双跑对比 SSIM 0.981——差异根因＝compose 依赖 macOS 系统字体，Linux 落 font-kit 兜底致长标题换行漂移。**新规则：compose 一律自带 woff2 字体（禁 local() 系统字体）**，下一部片按此双跑达帧级一致后切默认、记 ledger。知识包 `produce/references/server-render.md`，脚本 `tools/render-remote.sh` |

## 7/18 – 7/22 todo（M2 检查点：下周三）

- [ ] 7/18–20：首片按评分短板返工；发布后 24–48h 冷启动数据首轮回流（场景 D 归因：CTR→封面标题 / 完播→节奏 / 互动→选题）
- [ ] 7/18–20：`schema/film.schema.json`（从 M0 三片反推）+ `ir read / validate` 上线；film.json 直写拦截 hook
- [ ] 7/21：`ir patch`（乐观锁 + ledger 自动 append + 门控字段拒改）+ motion/anchors 任务卡模板（四要素 + 风格包切片位）
- [ ] 7/21：B 线第 2 条热点片（验证 48h 时效工序跑得动）；检索 API 接入（契约已到为前提）
- [ ] 7/22 **M2 检查点验收**：
  - 两方向各 ≥1 条已发布，评分 ≥ M0 基线（雅诗兰黛 4.29 为参照）
  - G2 评委在 storyboard / 成片两点试运行（与人工分并行记录，**不拿否决权**——先校准后放权）
  - 渲染 API 接入完成或给出明确阻塞点；冷启动数据回流报告 1 份
- 蓝图版 M2（Seedance 锚点全量 / 来源路由器 / 镜头级 QC 全量 / 评委全量）按 pull-based 逐项顺延，触发条件见 [film-ir-context-architecture.md §5](film-ir-context-architecture.md#5-落地顺序m1-内)

## 风险台账

| 风险 | 兜底 |
|---|---|
| 一天双片超产能 | 保 A 线完整出厂；B 线可停"成片待发布"，7/18 补发 |
| 检索 API 契约未到而 A 线需实拍 | Seedance 风格化空镜 / 人工投喂，决策留痕 |
| B 线荆华密算无专属风格包 | 现有包 + 品牌色覆写；专属包 M2 后走考片制新建 |
| 3 分钟片 film.json 手编易错 | IR API 顺延不减自查——storyboard 八项逐项过并记 gates |
| D8/D9 新维度首打分歧大 | 属预期；理由必须落到具体镜头/标题，积累即校准语料 |
